<Window x:Class="TaskManager.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TaskManager.UI"
        mc:Ignorable="d"
        Title="MainWindow" Width="1280" Height="720"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackgroundBrush}">

    <DockPanel>
        <Menu DockPanel.Dock="Top"
              Background="{StaticResource PrimaryBrush}"
              Foreground="{StaticResource ForegroundBrush}"
              Height="22"
              FontSize="12pt">
            <MenuItem Header="File">
                <MenuItem Header="New Task" Click="NewTask_Click"/>
                <MenuItem Header="Open" Click="OpenFile"/>
                <MenuItem Header="Export" Click="Export"/>
                <MenuItem Header="Settings"/>
            </MenuItem>
            <MenuItem Header="Edit">
                <MenuItem Header="Undo"/>
                <MenuItem Header="Redo"/>
                <MenuItem Header="Copy"/>
                <MenuItem Header="Paste"/>
                <MenuItem Header="Edit Task"/>
                <MenuItem Header="Delete Task"/>
            </MenuItem>
            <MenuItem Header="View">
                <MenuItem Header="Dark/Light Mode"/>
                <MenuItem Header="Refresh"/>
                <MenuItem Header="Show/Hide Completed Task"/>
                <MenuItem Header="Sort By">
                    <MenuItem Header="Date"/>
                    <MenuItem Header="Priority"/>
                </MenuItem>
                <MenuItem Header="Filter"/>
            </MenuItem>
        </Menu>

        <Border DockPanel.Dock="Top" Background="{StaticResource SecondaryBrush}" Padding="10,6" BorderThickness="0">
            <DockPanel>
                <Button DockPanel.Dock="Right"
                        Content="+ New Task"
                        Click="NewTask_Click"
                        Margin="10"
                        Padding="12,4"
                        Background="{StaticResource PrimaryBrush}"
                        Foreground="{StaticResource ForegroundBrush}"
                        BorderThickness="0"/>
                <WrapPanel>
                    <Border Style="{StaticResource FilterControl}">
                        <TextBox x:Name="Search" Text="Search"
                                 Width="200"
                                 Margin="10"
                                 Background="Transparent"
                                 BorderThickness="0"
                                 Padding="4"/>
                    </Border>
                    <ComboBox x:Name="cmbState" Width="120" Margin="10"/>
                    <ComboBox x:Name="cmbCategory" Width="120" Margin="10"/>
                    <ComboBox x:Name="cmbPriority" Width="120" Margin="10"/>
                    <ComboBox x:Name="cmbSort" Width="120" Margin="10"/>
                </WrapPanel>
            </DockPanel>
        </Border>

        <ScrollViewer Background="{StaticResource BackgroundBrush}">
            <ItemsControl x:Name="taskList">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Width="280" Height="260" Margin="10"
                                Background="{StaticResource AccentBrush}"
                                CornerRadius="12">
                            <StackPanel Margin="12">
                                <TextBlock Text="{Binding Title}" Foreground="{StaticResource ForegroundBrush}" FontSize="14" FontWeight="Bold"/>
                                <Separator/>
                                <TextBlock Text="{Binding Description}" Foreground="{StaticResource SubtextBrush}" FontSize="11" TextWrapping="Wrap"/>
                                <Separator/>
                                <TextBlock Text="{Binding Deadline}" Foreground="{StaticResource SubtextBrush}" FontSize="10"/>
                                <Separator/>
                                <TextBlock Text="{Binding Priority}" Foreground="{StaticResource SubtextBrush}" FontSize="10"/> 
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </DockPanel>

</Window>